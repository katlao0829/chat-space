.Mainchat__MessageList__Chat{data: {message: {id: message.id}}}
  .Mainchat__MessageList__Chat__User
    .Username
      = message.user.name
    .Timestamp
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .Chatmessage
    - if message.text.present?
      %p= message.text
    = image_tag message.image.url, class: 'Message__image' if message.image.present?